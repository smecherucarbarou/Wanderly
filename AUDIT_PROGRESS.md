# Wanderly — Audit Fix Progress
Auto-generated by Claude Code. Each entry = one completed fix.
Last session: 2026-06-29

### QW-1 + H-1 · Fix TABLE-returning RPC decode
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/ProfileRepository.kt:96, :109, :351-353, :462-464, :487-489
**What changed:** `accept_streak_loss`, `restore_streak`, and `admin_update_profile_stats` are TABLE-returning RPCs that serialize as a JSON array of rows, but were decoded with `.decodeRpc<T>()` (which expects a bare jsonb object and throws on an array). Switched all three to `.decodeList<T>().firstOrNull()` with a null-guard return — `SensitiveProfileMutationResult.ParseFailure` for the two streak mutations, `false` for the boolean-returning admin update. Also widened `StreakMutationRpcResponse` and `AdminStatsUpdateResponse` from `private` to `internal` so the regression test in the same module can reference them (matches the existing `internal` visibility of the sibling RPC response DTOs).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `compileDebugKotlin` executed (main edits compile), `RpcResponseDecodingTest` passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-15 · Regression test for the above
**Status:** Done
**Files changed:** app/src/test/java/com/novahorizon/wanderly/data/RpcResponseDecodingTest.kt:1-9 (imports), :180-238 (new tests)
**What changed:** Appended four tests to the existing `RpcResponseDecodingTest`. For both `StreakMutationRpcResponse` and `AdminStatsUpdateResponse`: (1) decoding a TABLE-shaped `[{...}]` payload as a single object via `SupabaseRpcJson.decodeFromString<T>` (the exact call `decodeRpc()` delegates to) throws `SerializationException`; (2) decoding the same payload as `List<T>` and taking `.firstOrNull()` succeeds and returns the first row with correct field values. Tests use the real `SupabaseRpcJson` and the now-`internal` DTOs, so they pin the production decode contract without constructing a `PostgrestResult`.
**Verified by:** `./gradlew :app:testDebugUnitTest --tests "...RpcResponseDecodingTest"` BUILD SUCCESSFUL — all tests in the class passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-2 · Fix place-ranking score formula
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/api/PlacesGeocoder.kt:133-136
**What changed:** The `score` sum had an unparenthesized `if (...) 0.2 else 0.0` whose `else` branch greedily absorbed the following `+ rating + reviewCount` terms (operator precedence). Result: when the type-match condition was true the score was only `nameMatch + 0.2` (rating/review weighting dropped); when false it was `nameMatch + 0.0 + rating + reviewCount`. Wrapped the `if/else` in parentheses so the rating and review-count weighting are always summed regardless of the type-match branch.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `PlacesGeocoder.kt` compiles; `PlacesGeocoderNameMatchTest` passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### MODEL · Gemini model string updated (edge function)
**Status:** Done
**Files changed:** supabase/functions/gemini-proxy/index.ts:15-16
**What changed:** Updated `DEFAULT_GEMINI_MODEL` from `gemini-3-flash-preview` to `gemini-3.5-flash` (GA model id, Google I/O 2026, 2026-05-19; REST `…/v1beta/models/gemini-3.5-flash:generateContent`). Per the user's follow-up constraint, set `DEFAULT_GEMINI_FALLBACK_MODEL` to `gemini-3-flash-preview` (Gemini 3 / "3.0" Flash — the only available Gemini 3 Flash id; Google docs state all Gemini 3 models are still preview, and this was the codebase's original primary). Primary and fallback are now distinct again. This file is a Supabase edge function; editing the repo does NOT deploy it — `supabase functions deploy gemini-proxy` is required for the change to reach the live AI.
**Verified by:** DEPLOYED to live via `npx supabase functions deploy gemini-proxy --project-ref kllgqwryipbegrnwfczo` — function version bumped 25 → 26 (2026-06-29 08:51 UTC), confirmed via `functions list`. Live AI now serves primary `gemini-3.5-flash`, fallback `gemini-3-flash-preview`.
**Timestamp:** 2026-06-29 (deployed; repo .ts in working tree, not yet committed)
---

### MODEL · Gemini model string updated (client log label)
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/mission/MissionPlaceSelector.kt:109
**What changed:** Updated the debug log label `model=gemini-3-flash-preview` to `model=gemini-3.5-flash`. This is a log string only (the actual model is chosen server-side by the edge function); it is now consistent with the edge-function default above.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — file compiles.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-3 · Atomic profile mutations
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/ProfileRepository.kt:28-29 (imports), and the read-modify-write sites in `adminUpdateProfileStats`, `updateProfileLocation`, `useStreakFreeze`, `purchaseShopItem`, `applyEquippedCosmetic`, `uploadAvatar`, `applyProgressSnapshot`
**What changed:** Replaced every non-atomic `_currentProfile.value = _currentProfile.value?.copy(...)` read-modify-write with atomic `MutableStateFlow.update { it?.copy(...) }` (or `updateAndGet` where the post-mutation value is returned/cached). Guards that depended on the previous value (id match in `adminUpdateProfileStats`/`uploadAvatar`, cosmetic `when` type) were moved inside the update lambda so the decision and the write are one atomic operation. In `applyProgressSnapshot` the suspend `getCurrentProfile()` base-load was hoisted out of the atomic block (it can't run inside `update {}`), then the delta is applied atomically against the current value. Return/cache sites now read the value returned by `updateAndGet` instead of re-reading `.value`. Plain overwrite assignments (`= profile`, `= normalized`, `= null`) were left unchanged — they are not read-modify-write races.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full unit suite (incl. ProfileRepository payload/avatar/gem-discovery tests) passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-4 · Make streakMessage a one-shot event
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/missions/MissionsViewModel.kt:59-62, app/src/main/java/com/novahorizon/wanderly/ui/missions/MissionsFragment.kt:135-138
**What changed:** Added `clearStreakMessage()` to `MissionsViewModel` (sets `_streakMessage.value = null`), mirroring the existing one-shot pattern already present in `MainViewModel`. The `MissionsFragment` observer now calls `viewModel.clearStreakMessage()` immediately after `showSnackbar(...)` and before `requestNotificationPermissionIfNeeded()`, so the snackbar isn't re-emitted on re-subscription (e.g. rotation).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — MissionsViewModel tests passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-9 · In-flight guard on completeMission and discoverGem
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/missions/MissionsViewModel.kt:37 (import), :65 (field), completeMission guard + finally; app/src/main/java/com/novahorizon/wanderly/ui/gems/GemsViewModel.kt:21 (import), :30 (field), discoverGem guard + try/finally
**What changed:** Added a `java.util.concurrent.atomic.AtomicBoolean` guard to each method (`completeMissionInFlight`, `discoverInFlight`). Each does `if (!flag.compareAndSet(false, true)) return` before launching the coroutine and resets `flag.set(false)` in a `finally` block, mirroring the `compareAndSet(false, true)` + `finally` reset pattern used by `purchaseShopItem`/`claimStreakMilestone` in `ProfileViewModel`. `discoverGem` previously had no try block, so its `when` was wrapped in `try { … } finally { … }` (no catch added — out of scope; exceptions still propagate). Chose `AtomicBoolean` (per the audit wording) over the `MutableStateFlow<Boolean>` variant used in `ProfileViewModel` because these guards are internal and don't need to be observed by the UI.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — MissionsViewModel + GemsViewModel tests passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-19 · @Volatile shared in-memory fields
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/PreferencesStore.kt:16-18, app/src/main/java/com/novahorizon/wanderly/data/ai/AiAssistantRepository.kt:111-113
**What changed:** Annotated `PreferencesStore.missionSnapshot` and `AiAssistantRepository.latestQuota` with `@Volatile` (kotlin.jvm.Volatile, default-imported) for cross-thread visibility. **Decision:** kept them as `@Volatile var` rather than promoting to `MutableStateFlow` — both are single nullable-reference get/set with no compound read-modify-write, so `@Volatile` provides the needed visibility guarantee without changing the field's API/consumers. Promote to `MutableStateFlow` only if a future requirement needs reactive observation or atomic compound updates.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full unit suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### H-5 + H-6 · Kill the idle foreground service
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/services/HiveRealtimeService.kt (onCreate guard, companion visibility), app/src/main/java/com/novahorizon/wanderly/MainActivity.kt:30 (import), startHiveService, app/src/main/AndroidManifest.xml:107 (removed <service>)
**What changed:** `ENABLE_PROFILE_REALTIME` is `false`, so `HiveRealtimeService` did nothing but post a permanent foreground notification. (1) Removed the `<service>` entry from AndroidManifest (replaced with a comment documenting how to restore it). (2) `MainActivity.startHiveService` now early-returns when `!HiveRealtimeService.ENABLE_PROFILE_REALTIME`, gating the start on the flag; the remaining `startForegroundService` is wrapped in try/catch for `IllegalStateException` (the supertype of `ForegroundServiceStartNotAllowedException`, which is API 31+ while minSdk is 26 — catching the supertype keeps it lint-clean) so a rejected FGS start logs instead of crashing. (3) `HiveRealtimeService.onCreate` now calls `stopSelf()` and returns immediately when the flag is off (defensive; no `startForeground` is invoked, and no `startForegroundService` launch path remains so the 5s-startForeground contract can't be violated). Made the companion + flag `internal` so `MainActivity` can gate on it. `stopService` callers (`MainActivity.stopHiveService`, `LogoutCoordinator`) are harmless no-ops against the now-undeclared service.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `processDebugManifest` succeeded (manifest valid); `HiveRealtimeService*Test` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-10 · Bound avatar input before readBytes()
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/AvatarRepository.kt:9 (import), uploadAvatar(uri,…) size pre-check, new queryAvatarSize() helper, MAX_RAW_AVATAR_BYTES constant
**What changed:** Added `queryAvatarSize(uri)` which reads `OpenableColumns.SIZE` via the `ContentResolver`. `uploadAvatar(uri, profileId)` now checks this before `readAvatarBytes(uri)` and returns `AvatarUploadResult.FileTooLarge` immediately when the raw size exceeds the new `MAX_RAW_AVATAR_BYTES` (25 MB), guarding against OOM from `readBytes()` pulling a huge file into memory. The recompressed output is still separately capped by the existing `MAX_AVATAR_UPLOAD_BYTES` (5 MB). The check is best-effort: providers that omit SIZE return null and fall through to the existing decode path.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `AvatarUploadLargeImageTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-5 · Drop dead admin_role reads
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/profile/ProfileFragment.kt (shouldShowDevPanel signature + 2 call sites), app/src/main/java/com/novahorizon/wanderly/ui/profile/DevDashboardFragment.kt:130 (removed admin row), app/src/test/java/com/novahorizon/wanderly/ui/profile/ProfileFragmentAvatarPresentationTest.kt:92 (test updated)
**What changed:** Removed the dead `isAdmin`/`profile.admin_role` parameter from `ProfileFragment.shouldShowDevPanel` (it was already `@Suppress("UNUSED_PARAMETER")` and the body only checks `isDebugBuild`); both call sites now pass just `BuildConfig.DEBUG`, and added the TODO: `admin state should come from a dedicated is_admin() RPC, not a client field`. Removed the "admin: yes/no" `DevDashboardRow` from `DevDashboardFragment`. Collapsed the now-redundant test assertions to the two meaningful cases (debug → shown, release → hidden). The `admin_role` field on the `Profile` data class was left intact (still part of the model; just no longer read by the UI).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — updated `ProfileFragmentAvatarPresentationTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-8 · Always load social home
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/social/SocialFragment.kt (onViewCreated deeplink block)
**What changed:** `onViewCreated` now calls `viewModel.loadSocialHome()` unconditionally on start. The pending deeplink invite code is consumed and handled *after* the home load (previously it was an either/or: a pending invite skipped the home load entirely). Combined with QW-11 below.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed (SocialFragment compiles).
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-11 · Confirm before sending friend request from deeplink
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/social/SocialFragment.kt (import + confirmDeeplinkFriendRequest), app/src/main/java/com/novahorizon/wanderly/data/SocialRepository.kt (findProfileByFriendCode), app/src/main/java/com/novahorizon/wanderly/data/WanderlyRepository.kt (delegate), app/src/main/res/values/strings.xml (2 strings)
**What changed:** A deeplinked friend code is no longer auto-submitted. New `SocialRepository.findProfileByFriendCode()` (exposed via `WanderlyRepository`) resolves the code through the `find_profile_by_friend_code` RPC and returns the `Profile` (or null). `SocialFragment.confirmDeeplinkFriendRequest()` looks up the code, and on success shows an `AlertDialog` ("Add friend?" / "Send a friend request to <username>?") with OK/Cancel; `viewModel.addFriend(code)` only runs on confirm. If the code resolves to nothing, a "Friend code not found" snackbar is shown. Added `social_add_friend_confirm_title`/`social_add_friend_confirm_message` strings (reusing the existing `social_friend_code_not_found`).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-14 · Remove unused firebase-analytics
**Status:** Done
**Files changed:** app/build.gradle.kts:471 (removed)
**What changed:** Deleted `implementation(libs.firebase.analytics)`. No source references `FirebaseAnalytics` (grep confirmed), and Crashlytics is a separate artifact (`libs.firebase.crashlytics`) that still resolves via the Firebase BOM.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — dependency graph resolved, Crashlytics still compiles.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-17 · Redact lat/lng in LogRedactor
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/observability/LogRedactor.kt (new RedactionPatterns object + coordinate redaction), app/src/main/java/com/novahorizon/wanderly/observability/CrashReporter.kt:46-47 (use shared patterns), app/src/test/java/com/novahorizon/wanderly/observability/LogRedactorTest.kt (new test)
**What changed:** Extracted the coordinate regexes into a shared `RedactionPatterns` object (same `observability` package, so both consumers use it without imports) — single source of truth so the two redactors can't drift. `CrashReporter.sensitiveValuePatterns` now references `RedactionPatterns.coordinateKey`/`coordinatePair`; `LogRedactor.redact()` now also replaces coordinate pairs with `[redacted-coords]`. Added a `redactHidesCoordinatePairs` test.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `LogRedactorTest` (incl. new coordinate test) + `CrashReporterTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-18 · Cap proxy error messages
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/api/GeminiClient.kt (2 sites), app/src/main/java/com/novahorizon/wanderly/api/PlacesProxyClient.kt:19 (constant + 1 site)
**What changed:** Truncated server-supplied error text to `ERROR_BODY_LOG_LIMIT` (512) before embedding it in a user-facing exception. In `GeminiClient`, both the `parseProxyErrorMessage(...)` result and the structured `error.message` are now `.take(ERROR_BODY_LOG_LIMIT)`. `PlacesProxyClient` had no such constant, so added `private const val ERROR_BODY_LOG_LIMIT = 512` (matching Gemini's value) and applied `.take(...)` to its parsed error message.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-13 · CI-driven versionCode
**Status:** Done
**Files changed:** app/build.gradle.kts:247
**What changed:** `versionCode = 1` → `versionCode = (System.getenv("VERSION_CODE")?.toIntOrNull() ?: 1)`. CI can now inject a monotonically increasing version code via the `VERSION_CODE` env var; local builds fall back to 1.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — Gradle config evaluated, build green.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-23 · Debug app-id suffix
**Status:** Done
**Files changed:** app/build.gradle.kts (debug block: applicationIdSuffix + versionNameSuffix)
**What changed:** Added `applicationIdSuffix = ".debug"` and `versionNameSuffix = "-debug"` to the debug build type so debug (`com.novahorizon.wanderly.debug`) and release can coexist on one device. Initially blocked because the Firebase `google-services.json` registered only the base package; the user then registered `com.novahorizon.wanderly.debug` in the Firebase project `wanderly-8b65a` and updated `google-services.json`, so `processDebugGoogleServices` now resolves the `.debug` client.
**Verified by:** `./gradlew :app:assembleDebug` BUILD SUCCESSFUL — `processDebugGoogleServices` passed and the debug APK packaged with the suffixed applicationId.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-24 · Populate the lint block
**Status:** Done
**Files changed:** app/build.gradle.kts:305 (lint block), app/lint-baseline.xml (new, generated), .github/workflows/ci.yml:108 (lint task)
**What changed:** Set `checkDependencies = true`, `checkReleaseBuilds = true`, `baseline = file("lint-baseline.xml")` in the previously-empty `lint {}` block. Generated `lint-baseline.xml` by running lint once (captures the current 6 errors + 80 warnings so they don't fail CI; new regressions will). Changed CI's lint step from `:app:lintDebug` to `:app:lint`. NOTE: one baselined error is `ForegroundServiceType` at `HiveRealtimeService.kt:97` — introduced by removing the `<service>` manifest entry in H-5/H-6; the `startForeground` call is now dead code (gated off), and the re-enable comment instructs restoring the manifest entry (with `foregroundServiceType`) if realtime is turned back on.
**Verified by:** `./gradlew :app:lint` BUILD SUCCESSFUL — "Lint found no new issues (and 6 errors and 80 warnings filtered by baseline lint-baseline.xml)".
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-21 · Replace blocking execute() with await()
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/api/PlacesProxyClient.kt (import + 2 call sites)
**What changed:** Replaced both `client.newCall(buildRequest(...)).execute()` calls with the existing `com.novahorizon.wanderly.util.await` suspend extension (`Call.await()`, already used by `GeminiClient`). `searchText` already runs in `withContext(Dispatchers.IO)`, so coroutine cancellation now cancels the in-flight socket instead of blocking the thread until completion.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `PlacesProxyClient` compiles; proxy URL + related tests passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-22 · Retry transient transport errors
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/api/GeminiClient.kt (imports + withRetry + isRetryableGeminiFailure)
**What changed:** `isRetryableGeminiFailure()` now also returns true for `SocketTimeoutException` and `IOException` (transient transport failures); `CancellationException` is still excluded because `withRetry` rethrows it before the classification call. Restructured `withRetry` to run all `maxAttempts` inside the single try/catch (`repeat(maxAttempts)` with an `attempt == maxAttempts - 1` guard) instead of doing the final attempt as a separate uncaught `block()` call — so the last attempt's errors get the same handling/semantics as the earlier ones.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `HttpRetryTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-26 · Delete unused parseDate()
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/widgets/StreakWidgetStateHelper.kt:202
**What changed:** Deleted the private `parseDate(rawDate)` function — grep confirmed it had no callers anywhere in the codebase.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `StreakWidgetStateHelperTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-27 · Delete unused uploadAvatar(imageBytes, mimeType) overload
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/AvatarRepository.kt:49-97 (removed)
**What changed:** Deleted the `uploadAvatar(imageBytes: ByteArray, mimeType: String)` overload. Grep confirmed no callers (all production + test call sites use the `uploadAvatar(uri, profileId)` overload). Shared helpers it used (`recompressAsJpeg`, `validateAvatarPayload`, `buildAvatarStorageTarget`) are still used by the surviving overload.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `ProfileViewModelTest`/`AvatarUploadLargeImageTest` (which use the uri overload) + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-28 · Delete PhotoVerificationDecision.verificationToken
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/util/AiResponseParser.kt:98, :109, app/src/test/java/com/novahorizon/wanderly/util/AiResponseParserTest.kt:96
**What changed:** Removed the unused `verificationToken` field from `PhotoVerificationDecision` and the corresponding parse assignment in `parsePhotoVerificationJson`. Dropped the now-irrelevant assertion in `AiResponseParserTest` (the test JSON still contains the key, which is harmlessly ignored).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — updated `AiResponseParserTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-29 · Delete ExampleInstrumentedTest
**Status:** Done
**Files changed:** app/src/androidTest/java/com/novahorizon/wanderly/ExampleInstrumentedTest.kt (deleted)
**What changed:** Deleted the default Android template instrumented test (only asserted the package name). No references elsewhere.
**Verified by:** File removed; it is androidTest-only boilerplate with no dependents (unit suite unaffected; BUILD SUCCESSFUL).
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-30 · Move analyzeImage compress/Base64 off the calling thread
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/api/GeminiClient.kt:66
**What changed:** Wrapped the JPEG `bitmap.compress(...)` + `Base64.encodeToString(...)` work in `withContext(Dispatchers.IO)` (chose the wrap option over deletion since `analyzeImage` is part of the gateway surface and is faked in androidTest's `TestSupport`). The blocking encode no longer runs on the caller's dispatcher.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — compiles; full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-32 · Rename util.SystemClock → RealClock
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/util/Clock.kt:7, and consumers WanderlyNotificationManager.kt, WanderlyStreakWidgetProvider.kt, AvatarRepository.kt, PreferencesStore.kt (import + `= RealClock`)
**What changed:** Renamed the `SystemClock` object to `RealClock` to stop it shadowing/colliding with `android.os.SystemClock`. Updated all four consumers' imports and `clock = RealClock` assignments. Left `StreakWidgetAlarmScheduler`'s `android.os.SystemClock.elapsedRealtime()` (the framework class) untouched.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL; grep confirms no remaining `util.SystemClock` references.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-33 · Detect duplicate-friend via SQLSTATE 23505
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/SocialRepository.kt:110
**What changed:** Replaced the brittle English text match (`contains("duplicate key") || contains("unique constraint")`) with a check for the Postgres unique-violation SQLSTATE `23505`, which is stable across locales/driver-message changes.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `SocialRepositoryFriendCodeTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-34 · Prune stale social.rival.* DataStore keys
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/notifications/NotificationStateStore.kt:58
**What changed:** Added `pruneRivalKeysExcept(today)`, called at the top of `markRivalMissionIfNew`. It removes any `social.rival.<day>.*` key whose day isn't today, so the per-rival-per-day flags can't accumulate unbounded across days; only the current day's flags are retained.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-35 · Bound GemsViewModel.seenGemsHistory
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/gems/GemsViewModel.kt (trimSeenGemsHistory + MAX_SEEN_GEMS)
**What changed:** After adding curated gem names to `seenGemsHistory`, `trimSeenGemsHistory()` evicts the oldest entries (insertion order, via the backing `LinkedHashSet`) once the set exceeds `MAX_SEEN_GEMS` (500). This caps the in-memory set so it can't grow unbounded across many cities/sessions while still deduping recent results.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `GemsViewModelTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-36 · Single widget refresh source
**Status:** Done
**Files changed:** app/src/main/res/xml/wanderly_widget_info.xml:9
**What changed:** Set `android:updatePeriodMillis` from `3600000` to `0`, so the system's periodic appwidget update no longer competes with the app's `StreakWidgetAlarmScheduler` (AlarmManager) refresh — AlarmManager is now the single refresh source. Added an XML comment documenting the decision.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `StreakWidgetAlarmSchedulerTest`/`StreakWidgetRefreshPolicyTest` + full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

## Follow-up batch — remaining improvements.md small wins (2026-06-29)

### QW-12 · Widget streak in UTC + future-date handling
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/widgets/StreakWidgetStateHelper.kt (now/today defaults → UTC, toEpochMillis → UTC), app/src/main/java/com/novahorizon/wanderly/streak/DailyStreakStatusEvaluator.kt, app/src/test/java/com/novahorizon/wanderly/widgets/StreakWidgetStateHelperTest.kt
**What changed:** The widget computed "today" from `LocalDateTime.now()` (device-local) while mission dates are UTC, so the widget streak disagreed with the app across the day boundary. Switched the helper's `now`/`today` defaults to `ZoneOffset.UTC`; also switched the private `toEpochMillis()` (used for the stale-snapshot check) from `ZoneId.systemDefault()` to `ZoneOffset.UTC` so the stale calc stays correct with a UTC `now`. In `DailyStreakStatusEvaluator`, a future `last_mission_date` (negative day diff from clock skew / timezones) now maps to `ACTIVE_TODAY` instead of falling through to `HARD_LOST`. Updated the two stale-indicator tests to compute `savedAtMillis` in UTC to match.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `StreakWidgetStateHelperTest` + `DailyStreakStatusEvaluatorTest` + full suite (354 tests) passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-16 · Reject NaN mission coordinates
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/mission/GooglePlacesMissionPlaceSearchService.kt
**What changed:** `parsePlaces` read `location.optDouble("latitude"/"longitude")`, which returns `NaN` when the field is absent, so a candidate with no location could be selected with garbage coords. Now parses with a `Double.NaN` default and drops the candidate (`return@mapNotNull null`) when either is NaN — mirroring the existing guard in `GooglePlacesDataSource.parsePlaces`.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-31 · Use post-snapshot balance for Completed honey
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/data/ProfileRepository.kt (logMissionCompletion)
**What changed:** `MissionCompletionResult.Completed` used `response.honey ?: 0`, collapsing a missing RPC field to 0. Now captures the `applyProgressSnapshot(...)` return (the authoritative merged profile, which falls back to the prior balance) and uses `updated?.honey ?: response.honey ?: 0` (same for streak count).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — full suite passed.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-7 · One LiveData mechanism in SocialViewModel
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/social/SocialViewModel.kt
**What changed:** Converted all 24 `.postValue(...)` calls to `.value = ...`. All run inside `viewModelScope.launch`/`collectLatest` (Main.immediate) — none inside a `withContext(IO)` block — so main-thread `setValue` is safe and consistent with the existing `.value =` writes (removes the post/set ordering hazard, audit M-3).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — `SocialViewModelTest` + full suite passed; grep confirms no `postValue` remains.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-20 · Expose state as asStateFlow()
**Status:** Done
**Files changed:** app/src/main/java/com/novahorizon/wanderly/ui/social/SocialViewModel.kt:49 (+import)
**What changed:** `val state: StateFlow<…> = _state` → `_state.asStateFlow()` so the public flow is read-only (matches `ProfileViewModel`).
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-25 · Bump Compose BOM, let it govern
**Status:** Done
**Files changed:** gradle/libs.versions.toml (composeBom 2025.05.00 → 2026.06.00; dropped version.ref on compose-material3 / compose-activity / compose-lifecycle-runtime)
**What changed:** Moved the Compose BOM to the latest stable (2026.06.00; Kotlin 2.3.x compatible — compiler is bundled with Kotlin) and removed the independently-pinned material3 / activity-compose / lifecycle-runtime-compose versions so the BOM governs them. The unused `material3`/`activityCompose`/`lifecycleRuntimeCompose` version entries were left in `[versions]` (harmless). No source changes were needed — Compose compiled clean against the new BOM.
**Verified by:** `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL (Compose screens + UI tests compile); `./gradlew :app:lint` — no new issues.
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---

### QW-6 / QW-37 / QW-38 · Cheap high-signal tests
**Status:** Done
**Files changed:** app/src/test/java/com/novahorizon/wanderly/data/ProfileVisibleColumnsTest.kt (new), app/src/test/java/com/novahorizon/wanderly/util/GeoMathTest.kt, app/src/test/java/com/novahorizon/wanderly/util/DateUtilsTest.kt
**What changed:** QW-6 — new `ProfileVisibleColumnsTest` asserts `PROFILE_VISIBLE_COLUMNS` excludes `last_lat/last_lng/admin_role/last_mission_date/last_buzz_date/updated_at` (and includes the safe columns). QW-37 — added GeoMath edges: antipodal (~20015 km), antimeridian crossing, equator crossing, same-pole coincidence, sub-100 m separation. QW-38 — added DateUtils cases: instant just before/after UTC midnight, and a negative-offset (America/New_York EDT) evening mapping to the next UTC day.
**Verified by:** `./gradlew :app:testDebugUnitTest` BUILD SUCCESSFUL — all new tests pass (354 total).
**Timestamp:** 2026-06-29 (working tree; not yet committed)
---
